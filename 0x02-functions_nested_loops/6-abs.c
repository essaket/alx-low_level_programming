#include "main.h"

/**
 * _abs - is a function that computes the absolute value of an integer
 *
 * @x: ckecks inüut of function
 *
 * * Return: value of n
 */

int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (-x);
}

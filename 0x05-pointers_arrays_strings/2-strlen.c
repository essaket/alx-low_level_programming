#include "main.h"
#include <string.h>

/**
 * _strlen - finction that return the length of a string
 *
 * @s: input of function
 *
 * Return: a int
 */

int _strlen(char *s)
{
	int x;

	x = strlen(s);

	return (x);
}
